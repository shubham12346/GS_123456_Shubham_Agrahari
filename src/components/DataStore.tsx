import ContainerWrapper from "../common/ContainerWrapper";

const DataStore = () => {
  return (
    <ContainerWrapper>
      <div className="  bg-white border-2 border-black h-full">
        this is data store
      </div>
    </ContainerWrapper>
  );
};

export default DataStore;
