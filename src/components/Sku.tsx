import React from "react";
import ContainerWrapper from "../common/ContainerWrapper";

const Sku = () => {
  return (
    <ContainerWrapper>
      <div>this is sku page</div>
    </ContainerWrapper>
  );
};

export default Sku;
